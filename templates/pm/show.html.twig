{% extends 'base.html.twig' %}

{% block title %}Pm{% endblock %}

{% block body %}
    <h1>Pm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pm.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ pm.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ pm.content }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ pm.date ? pm.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pm_index') }}">back to list</a>

    <a href="{{ path('pm_edit', {'id': pm.id}) }}">edit</a>

    {{ include('pm/_delete_form.html.twig') }}
{% endblock %}
