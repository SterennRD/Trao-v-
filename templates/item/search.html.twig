{% extends "base.html.twig" %}
{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.category, {'required': 'true', 'value' : '4', 'label' : 'Catégorie'}) }}
    {{ form_row(form.county, {'required': 'true', 'value' : '5'}) }}
    {{ form_row(form.dateBegin) }}
    <button class="btn btn-success">
        <i class="fa fa-check"></i>
        {{ button_label|default('Déposer mon annonce') }}
    </button>
    {{ form_end(form) }}
    {#<div class="container">#}
    {#<form action="{{ path('item_search_results') }}" method="get">#}
        {#<label>Catégorie</label>#}
        {#<select class="form-control" required>#}
            {#{% for category in categories %}#}
                {#<option value="{{ category.id }}">{{ category.label }}</option>#}
            {#{% endfor %}#}
        {#</select>#}

        {#<button type="submit" class="btn btn-default">Filtrer</button>#}
    {#</form>#}
        {#{% for item in items %}#}
            {#{{ item.name }} <br>#}
        {#{% endfor %}#}
    {#</div>#}
<div class="container">
    <h1>Rechercher un objet</h1>
    <form>
        <div class="form-group">
            <label>Catégorie</label>
            <select class="form-control" required>
                <option>Clés</option>
                <option>Portefeuille</option>
                <option>Jouet</option>
                <option>Animaux</option>
                <option>Electroniques</option>
                <option>Vêtements</option>
            </select>
        </div>
        <div class="form-group">
            <label>Département</label>
            <select class="form-control" required>
                <option>Ille-et-Vilaine</option>
                <option>Finistère</option>
                <option>Morbihan</option>
                <option>Côtes-d'Armor</option>
            </select>
        </div>
        <div class="form-group">
            <label>Date</label>
            <input type="date" class="form-control" required>
        </div>
        <button type="submit" class="btn btn-success">
            <i class="fa fa-search"></i>
            Rechercher
        </button>
    </form>
</div>
{% endblock %}